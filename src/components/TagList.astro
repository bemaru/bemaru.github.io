---
import { tagToSlug } from '../utils/tags';

interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<ul class="tag-list" aria-label="태그">
  {tags.map((tag) => (
    <li><a class="tag-link" href={`/tags/${tagToSlug(tag)}/`}>#{tag}</a></li>
  ))}
</ul>
